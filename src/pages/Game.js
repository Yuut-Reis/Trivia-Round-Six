import React, { Component } from 'react';
import Header from '../components/Header';

class Game extends Component {
  state = { }

  render() {
    return (
      <Header />
    );
  }
}

export default Game;
